name: GitHub Actions Demo
on: [push, check_run]
jobs:
  Null-Work:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: pwd
      - run: ls -ali
      - run: df -BM
      - run: ls -ali /
      - run: env
